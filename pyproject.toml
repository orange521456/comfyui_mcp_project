[project]
name = "comfyui-mcp"
version = "0.1.0"
description = "MCP Server that connects local ComfyUI to AI Agents"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "Pillow>=10.0.0",
]

[project.scripts]
comfyui-mcp = "src.server:main"